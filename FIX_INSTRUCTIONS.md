# 🔧 Сайтты түзету нұсқаулары

## Мәселе
Барлық сілтемелер `%20/` арқылы өтіп, жұмыс істемейді.

## Себебі
`cmd/build_site/main.go` файлында basePath дұрыс емес (`/` орнына `/mysitecheck` болуы керек).

## Шешім

### 1. Rebuild жасаңыз:
```bash
cd d:\golang-2
go run cmd/build_site/main.go
```

### 2. GitHub-қа жіберіңіз:
```bash
git add docs/
git commit -m "Fix broken links for GitHub Pages"
git push origin main
```

### 3. Күтіңіз (2-3 минут)
GitHub Pages автоматты түрде жаңартады.

### 4. Тексеріңіз:
https://meirbekashirbayev.github.io/mysitecheck/

---

## Не өзгерді?

**Бұрын:** `href="%20/"` → жұмыс істемейді  
**Енді:** `href="/mysitecheck/"` → дұрыс жұмыс істейді

Барлық сілтемелер қазір `/mysitecheck/` префиксімен қалыпты жұмыс істейді!
